[project]
name = "timestamps-to-midi-signals"
version = "0.5.0"
description = "Send MIDI signal from timestamps"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mido>=1.3.3",
    "python-rtmidi>=1.5.8",
    "rich>=14.0.0",
]
